{
  "migrations": [
    {
      "name": "0001_init",
      "sql": "CREATE TABLE IF NOT EXISTS leads (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, name TEXT NOT NULL, title TEXT, company TEXT, email TEXT, phone TEXT, website TEXT, location TEXT, linkedin TEXT, twitter TEXT, instagram TEXT, facebook TEXT, tiktok TEXT, youtube TEXT, github TEXT, status TEXT NOT NULL DEFAULT 'new', notes TEXT, created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL); CREATE INDEX IF NOT EXISTS idx_leads_user_name ON leads (user_id, name); CREATE TABLE IF NOT EXISTS lists (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, name TEXT NOT NULL, purpose TEXT, created_at INTEGER NOT NULL); CREATE INDEX IF NOT EXISTS idx_lists_user_name ON lists (user_id, name); CREATE TABLE IF NOT EXISTS lead_lists (lead_id TEXT NOT NULL, list_id TEXT NOT NULL, user_id TEXT NOT NULL, created_at INTEGER NOT NULL, PRIMARY KEY (lead_id, list_id)); CREATE INDEX IF NOT EXISTS idx_lead_lists_list ON lead_lists (list_id)"
    },
    {
      "name": "0002_messages",
      "sql": "CREATE TABLE IF NOT EXISTS messages (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, lead_id TEXT NOT NULL, platform TEXT NOT NULL, direction TEXT NOT NULL DEFAULT 'out', body TEXT NOT NULL, occurred_at INTEGER NOT NULL, created_at INTEGER NOT NULL); CREATE INDEX IF NOT EXISTS idx_messages_lead_time ON messages (lead_id, occurred_at)"
    },
    {
      "name": "0003_lead_source",
      "sql": "ALTER TABLE leads ADD COLUMN source TEXT"
    },
    {
      "name": "0004_fit_source_url_found_at_seq",
      "sql": "ALTER TABLE leads ADD COLUMN fit TEXT; ALTER TABLE leads ADD COLUMN source_url TEXT; ALTER TABLE leads ADD COLUMN found_at INTEGER; ALTER TABLE messages ADD COLUMN seq INTEGER NOT NULL DEFAULT 0"
    },
    {
      "name": "0005_needs_attention",
      "sql": "ALTER TABLE leads ADD COLUMN needs_attention INTEGER NOT NULL DEFAULT 0; ALTER TABLE leads ADD COLUMN attention_reason TEXT; ALTER TABLE leads ADD COLUMN attention_at INTEGER"
    },
    {
      "name": "0006_sources",
      "sql": "CREATE TABLE IF NOT EXISTS sources (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, name TEXT NOT NULL, kind TEXT NOT NULL DEFAULT 'Other', url TEXT, notes TEXT, created_at INTEGER NOT NULL); CREATE UNIQUE INDEX IF NOT EXISTS idx_sources_user_name ON sources (user_id, name COLLATE NOCASE); ALTER TABLE leads ADD COLUMN source_id TEXT; CREATE INDEX IF NOT EXISTS idx_leads_source ON leads (source_id); INSERT INTO sources (id, user_id, name, kind, created_at) SELECT lower(hex(randomblob(16))), user_id, MIN(n), MIN(k), MIN(created_at) FROM (SELECT user_id, created_at, CASE WHEN trim(source) LIKE 'Facebook groups: %' THEN trim(substr(trim(source), 18)) WHEN trim(source) LIKE 'Facebook group: %' THEN trim(substr(trim(source), 17)) ELSE trim(source) END AS n, CASE WHEN trim(source) LIKE 'Facebook group%' THEN 'Facebook group' ELSE 'Other' END AS k FROM leads WHERE source IS NOT NULL AND trim(source) <> '') GROUP BY user_id, n COLLATE NOCASE"
    },
    {
      "name": "0007_country",
      "sql": "ALTER TABLE leads ADD COLUMN country TEXT; CREATE INDEX IF NOT EXISTS idx_leads_country ON leads (user_id, country)"
    },
    {
      "name": "0008_projects_members",
      "sql": "CREATE TABLE IF NOT EXISTS projects (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, name TEXT NOT NULL, description TEXT, owner_name TEXT, created_at INTEGER NOT NULL); CREATE INDEX IF NOT EXISTS idx_projects_user ON projects (user_id); ALTER TABLE lists ADD COLUMN project_id TEXT; CREATE INDEX IF NOT EXISTS idx_lists_project ON lists (project_id); CREATE TABLE IF NOT EXISTS project_members (project_id TEXT NOT NULL, user_id TEXT NOT NULL, display_name TEXT, joined_at INTEGER NOT NULL, PRIMARY KEY (project_id, user_id)); CREATE INDEX IF NOT EXISTS idx_project_members_user ON project_members (user_id); CREATE TABLE IF NOT EXISTS project_invites (code TEXT PRIMARY KEY, project_id TEXT NOT NULL, created_by TEXT NOT NULL, expires_at INTEGER NOT NULL, created_at INTEGER NOT NULL); CREATE INDEX IF NOT EXISTS idx_project_invites_project ON project_invites (project_id); ALTER TABLE leads ADD COLUMN assigned_to_user_id TEXT; CREATE INDEX IF NOT EXISTS idx_leads_assigned ON leads (assigned_to_user_id)"
    },
    {
      "name": "0009_tags_fields_followups_history",
      "sql": "ALTER TABLE leads ADD COLUMN tags TEXT; ALTER TABLE leads ADD COLUMN custom_fields TEXT; ALTER TABLE leads ADD COLUMN next_action_at INTEGER; ALTER TABLE leads ADD COLUMN next_action TEXT; ALTER TABLE leads ADD COLUMN history TEXT; CREATE INDEX IF NOT EXISTS idx_leads_next_action ON leads (user_id, next_action_at)"
    },
    {
      "name": "0010_join_table_replacements",
      "sql": "CREATE TABLE IF NOT EXISTS lead_list_memberships (id TEXT PRIMARY KEY, lead_id TEXT NOT NULL, list_id TEXT NOT NULL, user_id TEXT NOT NULL, created_at INTEGER NOT NULL); CREATE UNIQUE INDEX IF NOT EXISTS idx_lead_list_memberships_pair ON lead_list_memberships (lead_id, list_id); CREATE INDEX IF NOT EXISTS idx_lead_list_memberships_list ON lead_list_memberships (list_id); CREATE TABLE IF NOT EXISTS project_memberships (id TEXT PRIMARY KEY, project_id TEXT NOT NULL, user_id TEXT NOT NULL, display_name TEXT, joined_at INTEGER NOT NULL, created_at INTEGER NOT NULL); CREATE UNIQUE INDEX IF NOT EXISTS idx_project_memberships_pair ON project_memberships (project_id, user_id); CREATE INDEX IF NOT EXISTS idx_project_memberships_user ON project_memberships (user_id); CREATE TABLE IF NOT EXISTS join_table_backfills (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, created_at INTEGER NOT NULL); CREATE UNIQUE INDEX IF NOT EXISTS idx_join_table_backfills_user ON join_table_backfills (user_id)"
    }
  ]
}
