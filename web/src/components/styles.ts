export const inputClass = 'mt-1 w-full rounded-xl border border-[var(--line)] bg-[var(--glass)] px-4 py-2.5 text-sm text-[var(--ink)] outline-none focus:border-[var(--accent)]'

/** Long unbroken text - a pasted URL, an id - wraps instead of widening its container. */
export const wrapAnywhere = 'min-w-0 [overflow-wrap:anywhere]'
